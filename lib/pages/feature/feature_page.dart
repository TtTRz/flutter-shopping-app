import "package:flutter/material.dart";

class FeaturePage extends StatelessWidget {
  const FeaturePage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text("Shopping Feature Page"),
    ));
  }
}
